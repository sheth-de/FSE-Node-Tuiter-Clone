/**
 * @file Declares Topic data type representing the topics associated with a tuit
 */
import Tuit from "./Tuit";

/**
 * @typedef Topic Represents topics associated with a tuit
 * @property {string} topic is the string associated with a tuit
 */
export default class Topic {
   private topic: string = '';
}