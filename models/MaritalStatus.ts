/**
 * @file defines the types of MaritalStatus entries allowed in database
 */
enum MaritalStatus {
   Married = 'MARRIED',
   Single = 'SINGLE',
   Widowed = 'WIDOWED'
};
export default MaritalStatus;

